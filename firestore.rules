rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global Safety Net
    match /{document=**} {
      allow read, write: if false;
    }
    
    // Helpers
    function isValidId(id) { 
      return id is string && id.size() <= 128; 
    }
    
    function incoming() { 
      return request.resource.data; 
    }
    
    function existing() { 
      return resource.data; 
    }

    function isSignedIn() {
      return request.auth != null;
    }

    // Schema Validation Helpers
    function isValidLead(data) {
      return data.id is string
        && data.parentName is string && data.parentName.size() <= 200
        && data.parentPhone is string && data.parentPhone.size() <= 64
        && data.childName is string && data.childName.size() <= 100
        && data.childSurname is string && data.childSurname.size() <= 100;
    }

    function isValidClient(data) {
      return data.id is string
        && data.parentName is string && data.parentName.size() <= 200
        && data.parentPhone is string && data.parentPhone.size() <= 64
        && data.childName is string && data.childName.size() <= 100
        && data.childSurname is string && data.childSurname.size() <= 100
        && data.status is string;
    }

    function isValidTask(data) {
      return data.id is string
        && data.title is string && data.title.size() <= 300
        && data.status is string
        && data.assignedTo is string;
    }

    function isValidGroup(data) {
      return data.id is string
        && data.name is string && data.name.size() <= 100
        && data.coachId is string;
    }

    function isValidCoach(data) {
      return data.id is string
        && data.name is string && data.name.size() <= 200;
    }

    function isValidFinance(data) {
      return data.id is string
        && data.type is string
        && data.category is string
        && data.amount is number;
    }

    function isValidMessage(data) {
      return data.id is string
        && data.senderRole is string
        && data.senderName is string
        && data.text is string;
    }

    function isValidTrainingSession(data) {
      return data.id is string
        && data.groupId is string
        && data.groupName is string
        && data.date is string
        && data.dateString is string
        && data.coachId is string
        && data.coachName is string
        && data.presentCount is number
        && data.sickCount is number
        && data.absentCount is number
        && data.records is list;
    }

    function isValidAccount(data) {
      return data.id is string
        && data.name is string
        && data.type is string
        && data.balance is number;
    }

    // Collections
    match /leads/{leadId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(leadId) && isValidLead(incoming());
      allow delete: if isSignedIn();
    }

    match /clients/{clientId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(clientId) && isValidClient(incoming());
      allow delete: if isSignedIn();
    }

    match /tasks/{taskId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(taskId) && isValidTask(incoming());
      allow delete: if isSignedIn();
    }

    match /groups/{groupId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(groupId) && isValidGroup(incoming());
      allow delete: if isSignedIn();
    }

    match /coaches/{coachId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(coachId) && isValidCoach(incoming());
      allow delete: if isSignedIn();
    }

    match /finances/{recordId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(recordId) && isValidFinance(incoming());
      allow delete: if isSignedIn();
    }

    match /messages/{messageId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(messageId) && isValidMessage(incoming());
      allow delete: if isSignedIn();
    }

    match /training_sessions/{sessionId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(sessionId) && isValidTrainingSession(incoming());
      allow delete: if isSignedIn();
    }

    match /accounts/{accountId} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && isValidId(accountId) && isValidAccount(incoming());
      allow delete: if isSignedIn();
    }

    match /notifications/{notificationId} {
      allow read, write: if isSignedIn();
    }

    match /systemUsers/{userId} {
      allow read, write: if isSignedIn();
    }

    match /_config/{configId} {
      allow read, write: if isSignedIn();
    }

    match /counterparties/{counterpartyId} {
      allow read, write: if isSignedIn();
    }

    match /test/{testId} {
      allow read, write: if isSignedIn();
    }
  }
}
